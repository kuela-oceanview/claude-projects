# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository

This is a collection of small web projects built as single self-contained HTML files (no build step, no dependencies). Open any `.html` file directly in a browser to run it.

## Workflow

- After every meaningful change, commit with a clear message and push to `origin/main`.
- Each project lives in its own `.html` file at the repo root.

## Current Projects

- `tictactoe.html` — 2-player local Tic Tac Toe with score tracking and win animation.
